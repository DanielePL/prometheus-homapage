# Everything public is meant to be found. The exceptions below are pages that
# exist for a person mid-flow, not for a search result:
#   /goodbye   what you see after deleting your account
#   /auth/     an OAuth return, renders nothing on its own
#   /stripe/   a payment return, same
#   /growth    the investor pitch — not secret, but not something we want
#              surfacing above the product when someone searches the brand
User-agent: *
Allow: /
Disallow: /goodbye
Disallow: /auth/
Disallow: /stripe/
Disallow: /growth

Sitemap: https://prometheus.coach/sitemap.xml
